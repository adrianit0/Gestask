# QA-002 - Tipos, fecha lÃ­mite, comentarios, scoring y ordenaciÃ³n

## Resultado
VerificaciÃ³n parcial. No se puede cerrar como pasada en este entorno porque falta ejecuciÃ³n real en navegador contra Supabase.

## Entorno disponible
- Frontend local con build Vite.
- CÃ³digo fuente SQL, Edge Functions y SPA.

## Entorno no disponible
- Supabase CLI.
- `psql`.
- `deno` para `deno check` de Edge Functions.
- Navegador conectado a un proyecto Supabase con migraciones y funciones desplegadas.

## Comprobaciones ejecutadas

| ComprobaciÃ³n | Resultado |
|---|---|
| `npm.cmd run build` | Pasa. |
| `git diff --check` | Pasa, solo avisos LF/CRLF normales de Windows. |
| RevisiÃ³n de `tasks-create` para `limit_date`, `ticket_type`, `comments` | Pasa por inspecciÃ³n. |
| RevisiÃ³n de `tasks-update` para reglas `Task`, PR y comentarios incrementales | Pasa por inspecciÃ³n. |
| RevisiÃ³n de `tasks-list` y `daily-report-get` para scoring y ordenaciÃ³n | Pasa por inspecciÃ³n. |
| RevisiÃ³n de UI para formulario, detalle, comentarios y ordenaciÃ³n | Pasa por inspecciÃ³n/build. |
| ValidaciÃ³n SQL real | Bloqueada: falta `psql`/Supabase CLI. |
| ValidaciÃ³n Edge Functions con Deno | Bloqueada: falta `deno`. |
| Prueba manual en navegador contra Supabase | Bloqueada: falta entorno Supabase desplegado. |

## Casos de prueba manual documentados

| Caso | Estado | Evidencia |
|---|---|---|
| Crear tarea sin `limit_date`; debe guardarse con `null`. | Verificado por cÃ³digo, no ejecutado en Supabase. | `tasks-create` normaliza vacÃ­o/null a `limit_date: null`; `script-003.sql` define `limit_date date null`. |
| Crear tarea sin `ticket_type`; debe guardarse como `Bug`. | Verificado por cÃ³digo, no ejecutado en Supabase. | `tasks-create` usa `body.ticket_type ?? "Bug"`; `script-003.sql` define default `Bug`. |
| Crear tarea `Task`; el selector PR solo permite `Not Finished`, `Need to Impute` e `Imputed`. | Verificado por build/cÃ³digo, no ejecutado manualmente. | `TASK_PR_STATUSES`; `TaskTable.js` usa catÃ¡logo reducido; `main.js` recalcula opciones al cambiar tipo. |
| Cambiar una tarea `Bug` a `Task`; un PR incompatible se normaliza. | Verificado por cÃ³digo, no ejecutado en Supabase. | `tasks-update` fuerza `Need to Impute` si `Done` y `Not Finished` si no `Done`; SQL redefine `normalize_task_state`. |
| AÃ±adir comentario desde detalle; al reabrir la tarea, el comentario sigue visible. | Verificado por cÃ³digo, no ejecutado en Supabase. | `TaskDetailModal` renderiza `comments`; `main.js` envÃ­a `comment`; `tasks-update` conserva comentarios existentes y aÃ±ade uno nuevo. |
| Ordenar por scoring descendente; las tareas con mayor scoring aparecen primero. | Verificado por cÃ³digo, no ejecutado en Supabase. | `taskSorting.ts` ordena por `scoring`; `tasks-list` y `daily-report-get` ordenan tras calcular scoring. |
| Ordenar por fecha lÃ­mite ascendente; tareas sin fecha lÃ­mite aparecen al final. | Verificado por cÃ³digo, no ejecutado en Supabase. | `taskSorting.ts` manda valores nulos/vacÃ­os al final. |
| Cambiar `scoring_prioridad`; el scoring cambia sin modificar tareas. | Verificado por cÃ³digo, no ejecutado en Supabase. | `configuration.ts` lee configuraciÃ³n efectiva del usuario cada vez que calcula scoring. |

## ConclusiÃ³n
La implementaciÃ³n estÃ¡ lista para QA manual real, pero `QA-002` no debe considerarse completada hasta ejecutar estos casos en navegador contra Supabase.

## Pasos pendientes para cerrar QA-002
1. Aplicar `supabase/sql/script-003.sql` sobre la base real.
2. Desplegar o servir Edge Functions actualizadas.
3. Abrir la SPA con sesiÃ³n vÃ¡lida.
4. Ejecutar manualmente los casos anteriores.
5. Si todos pasan, marcar `QA-002` como `Hecho` en `docs/03-tasks.md`.
